#Windows makefile

BUILD_DIR = ../../psp-projects_build/x86emu

#Output filename
TARGET = x86EMU.exe

TARGET := $(BUILD_DIR)/$(TARGET)

OBJS := $(patsubst %.o,$(BUILD_DIR)/%.o,$(OBJS))

CFLAGS = -I. -IC:\MinGW\include -IC:\MinGW\include\SDL

LIBS = -lSDL -lSDL_gfx

ifndef CC
CC = gcc
endif

$(BUILD_DIR)/%.o:%.c
	$(CC) $(CFLAGS) -o $@ -c $<

$(TARGET): $(OBJS)
	$(CC) $(LIBS) -o $(TARGET) $(OBJS)

win: $(TARGET) 
